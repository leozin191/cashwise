# Copy this file to .env (do not commit .env)
# For local dev, use your machine's LAN IP so the phone can reach the backend
# For production/EAS builds, set this in Expo project environment variables
EXPO_PUBLIC_API_URL=http://YOUR_LOCAL_IP:8080/api
